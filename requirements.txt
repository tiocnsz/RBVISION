# RBVISION 运行时依赖
# Python 3.10+
# 安装: pip install -r requirements.txt

# === GUI ===
PySide6>=6.5.0,<6.8        # Qt 6.5+ Python 绑定 (含 Qt 库,~200MB)

# === 3D 渲染 ===
vispy>=0.14.0              # PyOpenGL 高层封装,Qt 嵌入友好

# === 图像 / 标定 ===
opencv-python>=4.8.0        # 仿射 / 单应性 / 图像处理
numpy>=1.24.0
scipy>=1.10.0               # 最小二乘 / 多项式拟合

# === 通信 ===
pymodbus>=3.6.0             # ModbusTCP Server/Client

# === 3D 模型 (可选) ===
urdfpy>=0.0.20              # URDF 加载 (最新版 0.0.22)

# === 配置 / 日志 ===
PyYAML>=6.0
loguru>=0.7.0

# === 网络 (GigE 推流用) ===
# 标准库已含 socket/struct,如需高级可用 netifaces (可选)
